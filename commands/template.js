module.exports = {
  commands: ["template"],
  expectedArgs: "",
  permissionError: "You need admin permissions to run this command",
  minArgs: 0,
  maxArgs: 0,
  callback: (message, args, text) => {
    //Task to do
  },
  permissions: [],
  requiredRoles: [],
}
