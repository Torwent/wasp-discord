version: "3.9"

services:
  wasp-discord:
    container_name: "wasp-discord"
    build:
      context: .
    restart: unless-stopped
    networks:
      - wasp-server_proxy

networks:
  wasp-server_proxy:
    external: true
